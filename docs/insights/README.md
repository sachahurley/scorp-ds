# Insights

One finding per file, each tied to the run or audit that produced it. Insights are
**observations with a source**, not decisions: when an insight forces a choice, the
choice gets a record in `../decisions/` and links back here.

Naming: `YYYY-MM-DD-short-title.md`.
